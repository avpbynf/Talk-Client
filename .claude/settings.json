{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/branch-gate.sh\"; if [ -f \"$f\" ]; then sh \"$f\"; else exit 0; fi",
            "timeout": 10,
            "statusMessage": "Checking the push target"
          }
        ]
      }
    ]
  }
}
